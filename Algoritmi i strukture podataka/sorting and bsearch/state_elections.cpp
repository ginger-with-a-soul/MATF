#include <iostream>
#include <map>
#include <algorithm>
#include <vector>

using namespace std;

struct Birac {
	string sifra;
	string opstina;
};

int main(int argc, char** argv){

	unsigned n; cin >> n;
	
	vector<Birac> biraci(n);
	for(int i = 0; i < n; i++){
		cin >> biraci
	}

	exit(EXIT_SUCCESS);
}